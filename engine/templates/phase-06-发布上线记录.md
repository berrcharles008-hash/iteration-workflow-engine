# 上线记录

## 上线时间

{YYYY-MM-DD HH:mm}

---

## 阶段1：测试服务器部署

### 1.1 上线文件清单

| # | 文件路径 | 操作（新增/修改/删除） |
|---|---------|---------------------|

### 1.2 部署步骤

{Agent 按 deploy-config.yaml stage1_auto 执行并记录}

### 1.3 数据库变更（如有）

{DDL/DML 执行记录}

### 1.4 配置变更（如有）

### 1.5 部署结果

{成功/失败，失败原因}

---

## 阶段2：客户服务器部署

> 本章节由 Agent 在 step-1.5-deploy-stage2 执行时自动填充。
> 若 `stage2_manual.enabled==false`，本章节为空、步骤标记为 `not_applicable`。

### 2.1 部署前环境确认

| 检查项 | 状态 | 确认人 | 时间 |
|--------|:----:|--------|------|
| 数据库备份已完成 | ⬜ | | |
| 停机窗口已确认 | ⬜ | | |
| 部署人员具有访问权限 | ⬜ | | |

### 2.2 文件部署清单

> 变更文件清单来源：04-开发实现/04-任务清单.md → git diff → 用户手动提供

| # | 源文件（构建输出） | 目标路径 | 操作 | 备份位置 |
|---|-------------------|---------|:----:|---------|
| {由 Agent 从 step-1.2 + 任务清单自动填充} | | 覆盖 | {backup_dir}/{ITERATION_ID}/ |

### 2.3 部署步骤执行记录

| 步骤 | 操作描述 | 执行人 | 时间 | 结果 |
|:----:|---------|--------|------|:----:|
| 1 | 停止 IIS 应用程序池 | | | ⬜ |
| 2 | 备份现有文件到 {backup_dir}/{ITERATION_ID}/ | | | ⬜ |
| 3 | 部署新文件到 {customer_app_dir} | | | ⬜ |
| 4 | 启动 IIS 应用程序池 | | | ⬜ |

### 2.4 部署后验证

| 验证项 | 预期结果 | 实际结果 | 通过 |
|--------|---------|---------|:----:|
| 系统版本号 | {version} | | ⬜ |
| 核心流程冒烟测试 | 正常 | | ⬜ |
| 应用日志检查 | 无异常报错 | | ⬜ |

### 2.5 回滚方案

> 基于 step-1.2 的 backup_manifest.yaml 自动生成具体命令

```bash
# 恢复备份
xcopy "{backup_dir}\{ITERATION_ID}\*" "{customer_app_dir}" /E /Y
# 重启应用池
appcmd recycle apppool /apppool.name:{app_pool}
```

### 2.6 部署结论

⬜ 部署成功 / ⬜ 部署失败（原因：___）

---

## 上线后验证（通用）

{登录验证/功能验证/性能验证/数据验证}

## 上线结论

✅ 已上线 / ❌ 未上线

{简要说明}

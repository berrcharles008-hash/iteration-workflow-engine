# 技术方案设计模板

> 创建时间：YYYY-MM-DD
> 目标：一句话描述改造目标
> 影响等级：🔵 Standard / 🟡 Normal / 🔴 Emergency（见 `engine/gate-protocol.md`）

## 一、现状与痛点 *必填

> 描述当前实现的问题、用户痛点、为什么要改。1-3 段即可。

## 二、改造方案 *必填

### 2.1 新增数据库表（如有）

> 若涉及新表，列出表名、字段、索引。若不涉及，写"无"。

### 2.2 数据流设计 *必填

> 用文字或 ASCII 图描述数据流向：前端 → API → BLL → DB。复用已有 API 时注明。

### 2.3 页面设计（如有）

> 若涉及新页面或页面改造，描述布局、交互。若无前端改动，写"无"。

## 三、代码改动范围 *必填

> 使用 Delta 标记体系（详见 `engine/delta-marking.md`）：
> ADDED（新增）/ MODIFIED-🔵追加 / MODIFIED-🟡修改 / MODIFIED-🔴替换 / REMOVED

### 3.1 前端新增

| # | 文件路径 | Delta标记 | 说明 |
|---|---------|:---------:|------|

### 3.2 前端修改

| # | 文件路径 | Delta标记 | 说明 |
|---|---------|:---------:|------|

### 3.3 前端不动

> 列出明确不动的关键文件，避免误改。

### 3.4 后端新增/修改

| # | 文件路径 | Delta标记 | 说明 |
|---|---------|:---------:|------|

### 3.5 前端三层实现指引（Net/BLL/Domain）

> 若涉及前端三层，列出各层新增/修改的方法签名。

## 三.1 命名冲突预检 *必填

> 对所有 MODIFIED-追加 类型的目标文件，执行 `search_content` 验证新方法名不冲突。
> 详见 `engine/naming-conflict-check.md`。
> ★ 预检完成后，🟡🔴 方案将进入独立 Agent 交叉审查（`engine/cross-review-protocol.md`）。

| 目标文件 | 搜索方法名 | 结果 | 处理 |
|---------|-----------|:----:|------|

## 四、后端 API 设计（如有）

### 4.1 接口总览

> 列出新涉及的 API：路径、方法、用途。若无新 API，写"无"。

### 4.2 文件清单

> 列出涉及的后端文件：Entity/IMgr/BLL/Web 各层。

### 4.3 各层代码示例

> 关键方法的代码骨架（非完整实现），帮助 04 阶段 Agent 理解意图。

## 五、开发阶段

### Sprint 0-4 任务拆解

> 粗粒度拆解，详细任务清单在 04 阶段生成。按依赖关系分 Sprint。

## 六、回滚方案（🟡🔴 等级强制）

> Normal/Emergency 等级必填，Standard 等级可选。
> 详见 `engine/gate-protocol.md` 回滚方案模板。

| 维度 | 内容 |
|------|------|
| 回滚触发条件 | {什么情况下需要回滚} |
| 回滚操作步骤 | 1. {第一步} 2. {第二步} 3. {第三步} |
| 回滚验证方法 | {如何确认回滚成功} |
| 预估回滚耗时 | {分钟数} |
| 回滚责任人 | {执行者} |

# 独立 Agent 交叉审查协议

> 从 `workflow-engine.md` 提取。定义独立 Agent 交叉审查的通用规范。
> 适用于 02（🔴 强制 / 🟡 可选）/03（强制🟡🔴）/04（强制🟡🔴）阶段。

---

## 一、什么是交叉审查

独立 Agent（code-explorer 子代理）以"第三方 reviewer"视角审查主 Agent 的产出，消除作者盲区。

| 维度 | 主 Agent 自审 | 独立 Agent 交叉审查 |
|------|:---:|:---:|
| 执行者 | 产出文档的同一 Agent | 独立的 code-explorer 子代理 |
| 上下文 | 完整的对话上下文 + 代码库 | 仅读取文档和代码库文件（无对话上下文，无先入为主） |
| 盲区 | 存在——作者容易跳过"我认为没问题"的部分 | 无——不参与产出，无先入为主 |
| 适用场景 | 业务逻辑、设计意图验证 | 纯文件/规范/命名/一致性检查 |

---

## 二、触发时机

| 阶段 | 步骤ID | 触发条件 | 审查对象 |
|------|--------|:--:|------|
| 02-需求评审 | step-1x-review | 🔴（强制，外部模型优先）/ 🟡（可选） | 需求文档格式完整性 |
| 03-技术方案 | step-3x-cross-review | 🟡🔴（强制） | 技术方案 L1/L2 |
| 04-开发实现 | step-1-5x-cross-review | 🟡🔴（强制） | 开发任务清单 |

> ★ **步骤 ID 以 `phase-steps.md`（步骤清单唯一真相源）为准**。历史 `state.yaml` 曾出现两种写法
> （`step-1x-cross-review` ← 旧 `phase-02.md` 口径；`step-1x-review` ← SSOT 口径，2026-07-18 v1.2
> 因外部模型路由优先而更名，见 `phase-steps.md` §变更记录）。**落库一律用 `step-1x-review`**。
>
> ★★ **本表的"触发条件"≠ `mandatory`（两个维度，勿混）**：本表只定义"**应当执行**"；
> 而 `phase_steps[].mandatory` 的**唯一权威源是 `complexity-scoring.md` §二「步骤 mandatory 默认值表」**
> （该表明确"Agent 只能读取不能修改"）。其中 **02 的交叉审查 = `mandatory=false`**（🟡/🔴 均如此）
> ⇒ 🔴 应执行、但**不阻塞阶段推进**；03/04 的交叉审查 = `mandatory=true`（真强制）。

> 🟢 简单需求跳过交叉审查——改动量极小（1-2文件、修bug、改label），独立Agent审查的 token 成本 > 收益。

### 二-A. 审查 token 预算

> 以下预算为设计指引（guideline），非硬限制。Agent 无法在输出中途物理截断报告。
> 超出预算时，在报告末尾标注"⚠️ 本次审查超出预算，仅覆盖前 N 项"，不得尝试物理截断输出。

| 复杂度 | 单次审查预算 | 超出时行为 |
|--------|:--------:|------|
| 🟢 简单 | 跳过交叉审查 | 不执行独立 Agent 审查 |
| 🟡 中等 | ~15K tokens | 报告末标注"⚠️ 超出预算"，列出已覆盖/未覆盖的审查维度 |
| 🔴 复杂 | ~30K tokens | 报告末标注"⚠️ 超出预算"，提示可分段异步继续审查（先处理致命/严重，🔵/💡 留待补充审查） |

### 二-B. 并行分片与每路预算（★ 2026-09-20 新增）

> **背景**：单 code-explorer 审查实证 25~127 次工具调用 / 237~363s（2026-09-11~09-17 多轮记录）。
> 瓶颈拆解：**"无锚点盲目全库探索"是主因，"串行"是次因** ⇒ 只加并发不改喂料方式，token 总量反而上升。

| 复杂度 | 分片数 | 每路工具调用上限 | 总 token 预算 |
|:--:|:--:|:--:|:--:|
| 🟢 简单 | 0（跳过） | — | — |
| 🟡 中等 | 2 路 | 30 次 | ~15K（**合计**，约 7.5K/路） |
| 🔴 复杂 | 2~4 路 | 40 次 | ~30K（**合计**，约 7.5~15K/路） |

> **与 §二-A 的口径关系**：§二-A 的 token 预算在多路模式下即为**全片合计上限**（不是每路各算一次）；
> 两个单位职责不同 —— **token = 产出预算**，**工具调用次数 = 探索预算**（单片硬闸，超出即停）。

**分片维度（二选一，主 Agent 明示）**：
- **按模块/目录切片**：前端片 / 后端片 / SQL 片（跨端迭代适用）
- **按审查维度切片**：L1 命名冲突片 / L2 符号验证片（含路径一致性，原独立 L3 已并入 L2）

> ★ **完整性/覆盖率类维度不分片**：如"方案改动范围表每个文件在清单中都有对应任务"这类**全集对账**，
> 需要全局视角，任何单片都做不到 ⇒ 由**主 Agent 自审**承担，或单设一个"全局对账片"（见 §六）。

**每路硬约束**：
- **审查对象仍是全文**（方案 / 清单），锚点包约束的是**主动探索范围** —— 禁止无目标全库 `rglob`，
  不裁剪审查对象，免得片内缺上下文而审不了
- 只提供**本片**相关锚点，不把整份改动范围表复制给每一路
- 超出调用上限即停，报告末标注"⚠️ 本片超出预算，未覆盖：{维度}"
- 每条结论**必须带 `文件:行号` 或方案/清单章节引用**（如 `方案 §3.2`）；**两者皆无**才视为无效发现

---

## 三、审查维度（分级标准）

> 本分级用于独立 Agent 审查报告。03 阶段主 Agent 的方案评审使用另一套简化分级（🔴阻塞/📝注意），用途不同，见 `workflow-engine.md` §评审边界与通过标准。

| 级别 | 含义 | 处理方式 |
|:--:|------|---------|
| 🔴 致命 | 会导致编译失败/数据错误/安全漏洞/方案不可执行 | 必须修复 |
| 🟡 严重 | 逻辑矛盾/命名不一致/遗漏关键步骤 | 必须修复 |
| 🔵 轻微 | 格式问题/描述不清/可优化但不影响功能 | 可选修复 |
| 💡 建议 | 改进建议/更好的做法 | 记录，不强制 |

---

## 三-A. 锚点包（★ 派发前必附，2026-09-20 新增）

> **解决的问题**：子 Agent 只拿到"方案路径 + 项目根"，为验证一个符号要 `rglob` 全库找路。
> 主 Agent 在 03/04 已探索过，手里有现成的落点信息 —— **喂给子 Agent，让它只做定点核验**。

**主 Agent 派发前必须组装锚点包**（来源 = 03 方案「代码改动范围」表 / 04 任务清单）：

| 待验符号/文件 | 绝对路径 | 已知行号 | 验证要点 |
|---|---|---|---|
| `XxxMgr.BatchInsert` | `back-end/.../BLL/XxxMgr.cs` | 未新增（待 ADDED） | 是否已有同名成员 |

**派发 prompt 必须包含以下硬约束**：

```
★ 锚点包已附于下方。核验规则：
1. 先按锚点给出的 绝对路径 + 行号 定点读取，直接判定；
2. 锚点缺失/未命中时，才允许 1 次定向 search_content（带目录 glob）；
3. ★ 禁止无目标全库 rglob / 无 glob 的全局 search_content；
4. 每条结论必须给出 文件:行号 证据；无行号 = 无效发现；
5. 工具调用达到上限 {N} 次即停，报告末标注未覆盖的审查维度。
```

> **收益**：token 与墙钟双降，且结论可核（带行号）；避免"并发加剧重复探索"。

---

## 四、各阶段审查指令模板

### 4.1 03-技术方案 L1/L2 交叉审查

主 Agent 派发独立 Agent 时使用以下指令模板：

```
你是一个独立的代码审查Agent（"第三方 reviewer"），请以批判性视角审查以下技术方案文档，
找出主Agent（方案作者）可能遗漏的问题。

审查对象：{{方案文档路径}}
相关代码库：{{PROJECT_ROOT}}
本片范围：{{分片维度 + 本片覆盖的模块/目录}}

## 锚点包（主 Agent 填入，★ 必填）

| 待验符号/文件 | 绝对路径 | 已知行号 | 验证要点 |
|---|---|---|---|
| {{符号}} | {{路径}} | {{行号}} | {{要点}} |

★ 核验规则：1) 按锚点给定路径+行号定点读取；2) 锚点未命中时才允许 1 次**带目录 glob** 的 search_content；
3) 禁止无目标全库 rglob ／ 无 glob 的全局 search_content；4) 每条结论须给 `文件:行号` 或章节引用（如 `方案 §X`）；
5) 工具调用达 {{上限}} 次即停，并标注未覆盖的审查维度。

## 审查清单

> 本 L1/L2 审查是独立 Agent 的**二次验证**，不替代方案输出前主 Agent 的命名冲突预检。

### L1：命名冲突扫描
- [ ] 方案中每个 ADDED 符号（类名/方法名/属性名），搜索目标文件中是否已存在同名符号
- [ ] 如果有同名冲突，标注冲突位置和符号名

### L2：关键符号验证（内部自洽 + 代码库交叉验证，最多5项）
- [ ] 方案"代码改动范围"表中每个引用类型（基类/接口/异常类/DTO），确认在代码库中存在
- [ ] 文件路径是否与实际项目结构一致
- [ ] 从改动范围表提取优先级1符号（异常类、IMgr方法）→ search_content 确认存在
- [ ] 从改动范围表提取优先级2符号（基类、路由名）→ search_content 确认存在

## 输出格式

请按以下格式输出审查报告，表格后附审查总结：

| # | 级别 | 位置 | 问题描述 | 建议修复 |
|---|:--:|------|---------|---------|
| 1 | 🔴/🟡/🔵/💡 | 方案文档 §X / 代码文件:行号 | ... | ... |

审查总结：共发现 X 个问题（🔴致命 Y / 🟡严重 Z / 🔵轻微 W / 💡建议 V）
```

### 4.2 04-任务清单交叉审查

主 Agent 派发独立 Agent 时使用以下指令模板：

```
你是一个独立的代码审查Agent（"第三方 reviewer"），请审查以下开发任务清单，
逐项对照技术方案检查是否有遗漏或不一致。

审查对象：{{任务清单路径}}
对照基准：{{技术方案路径}}
相关代码库：{{PROJECT_ROOT}}
本片范围：{{分片维度 + 本片覆盖的模块/目录}}

## 锚点包（主 Agent 填入，★ 必填）

| 待验符号/文件 | 绝对路径 | 已知行号 | 验证要点 |
|---|---|---|---|
| {{符号}} | {{路径}} | {{行号}} | {{要点}} |

★ 核验规则：1) 按锚点给定路径+行号定点读取；2) 锚点未命中时才允许 1 次**带目录 glob** 的 search_content；
3) 禁止无目标全库 rglob ／ 无 glob 的全局 search_content；4) 每条结论须给 `文件:行号` 或章节引用（如 `方案 §X`）；
5) 工具调用达 {{上限}} 次即停，并标注未覆盖的审查维度。

## 审查清单

- [ ] 方案"代码改动范围"表中每个文件，任务清单中都有对应任务？
- [ ] 任务描述中的文件路径/类名/方法名是否与方案一致？
- [ ] ADDED 类型的任务描述中，是否明确声明了命名空间和继承关系？
- [ ] 任务依赖关系是否与方案的阶段划分一致？

## 输出格式

请按以下格式输出审查报告，表格后附审查总结：

| # | 级别 | 问题描述 | 方案引用 | 任务清单引用 |
|---|:--:|---------|---------|------------|
| 1 | 🔴/🟡/🔵/💡 | ... | 方案 §X | 任务 T... |

审查总结：共发现 X 个问题（🔴致命 Y / 🟡严重 Z / 🔵轻微 W / 💡建议 V）
```

### 4.3 02-需求格式完整性检查（🔴 应执行 / 🟡 可选）

主 Agent 派发独立 Agent 或组装外部模型 prompt 时使用以下指令模板：

```
你是一个独立的需求审查Agent，请检查以下需求文档的格式完整性（不评审业务合理性）。

## 审查对象

以下为需求文档完整内容（如有多份文档，用 --- 分隔）：

{{嵌入需求文档的完整文本内容}}

## 审查清单

- [ ] 需求标题、背景、范围是否完整？
- [ ] 是否有明确的验收标准？
- [ ] 边界场景是否描述清楚？
- [ ] 是否有遗漏的必填字段？
- [ ] 各文档之间是否自洽（无内部矛盾）？
- [ ] 架构分层（如 engine/ 与 project/）职责是否清晰无重叠？

## 输出格式

| # | 级别 | 问题描述 |
|---|:--:|---------|
| 1 | 🔴/🟡/🔵/💡 | ... |

审查总结：共发现 X 个问题（🔴致命 Y / 🟡严重 Z / 🔵轻微 W / 💡建议 V）
```

> ⚠️ 注意：主 Agent 替换 `{{嵌入需求文档的完整文本内容}}` 时必须嵌入文档的实际内容（通过 Read 读取），不能仅提供文件路径。外部模型无文件系统访问权限，只给路径会导致审查结果为空或推理链失控。

---

## 五、主 Agent 合并规则

独立 Agent 返回审查报告后，主 Agent 按以下规则合并处理：

| 独立Agent发现 | 处理 |
|:--|------|
| 🔴 致命 | 主 Agent **立即修复**，修复后主 Agent 自检确认（不重新启动独立 Agent） |
| 🟡 严重 | 主 Agent **立即修复**，修复后主 Agent 自检确认（不重新启动独立 Agent） |
| 🔵 轻微 | 主 Agent 评估后**选择性修复**，记录处理决定 |
| 💡 建议 | 记录，不阻塞阶段推进 |
| 独立Agent标记但实际无误 | 主 Agent 标注"误报"，记录理由 |

> ⚠️ **边界场景**：修复后**不需要重新启动独立 Agent 审查**——主 Agent 自检确认即可（否则会无限循环）。
>
> ⚠️ **误报裁定权**：用户拥有最终裁定权。如用户认为某项误报标记不当，主 Agent 必须重新评估。

### 合并输出格式

```
## 交叉审查结果（独立 Agent）

| # | 级别 | 发现问题 | 处理 |
|---|:--:|---------|------|
| 1 | 🔴 | Xxx不存在 | ✅ 已修复 |
| 2 | 🟡 | 路径不一致 | ✅ 已修复 |
| 3 | 🔵 | 描述可优化 | 📝 已记录，不阻塞 |
| 4 | 💡 | 建议使用Xxx模式 | 📝 已记录，暂不处理 |

独立Agent交叉审查完成：共 4 项，🔴0 🟡0 🔵1 💡1（除误报外已全部处理）
```

---

## 六、并行执行约定（★ 2026-09-20 扩展为 N 路分片）

**执行序**：
1. 主 Agent 组装**锚点包**并按 §二-B 切片 → 派发 N 路独立 Agent（异步 `task`，同一批发出）
2. 派发后主 Agent **立即**执行自身自审（不等待子 Agent）
3. 全部返回后主 Agent 执行**汇总**（见下）
4. 合并后的修复完成后，才进入下一步：
   - **03 阶段**：进入 step-4-review-gate（评审边界检查）
   - **04 阶段**：进入 step-1-6-user-confirm（用户确认任务清单）

**汇总规则（多路新增）**：

| 情形 | 处理 |
|:--|:--|
| 同一 `文件:行号` 被多路重复报告 | 去重，保留描述最完整的一条 |
| 多路结论相反 | 以**行号证据**为准；证据都充分仍矛盾 → 主 Agent 亲自 `read_file` 裁定 |
| 某片超预算未覆盖 | 汇总表标注"⚠️ {维度} 未覆盖"，主 Agent 自审兜底该维度 |
| **完整性/覆盖率类维度** | 不分片，由主 Agent 自审或单设"全局对账片"承担（§二-B） |
| 发现只有章节引用、无行号 | **受理**（整体性问题天然无单一行号），由主 Agent 亲自核实后定级 |
| 既无行号又无章节引用 | 丢弃（§二-B 硬约束） |
| 疑似误报 | 按 §五 裁定权处理（用户拥有最终裁定权） |

> ⚠️ **步骤清单中的序号不代表执行顺序**：step-3x-cross-review / step-1-5x-cross-review 在步骤表中排在 step-3-self-review / step-1-5-review 之后，但这仅是为了编号有序。实际执行时，独立 Agent 互审与主 Agent 自审**必须同时启动**。03/04 阶段均适用此规则。
>
> ⚠️ **分片不是越多越好**：上下文隔离会让同一文件被多路重复读取。🟡 用 2 路、🔴 用 2~4 路为宜；**先给锚点，再谈分片** —— 无锚点的分片只会成倍放大重复探索。

---

## 七、复杂度门禁

| 阶段 | 🟢 简单 | 🟡 中等 | 🔴 复杂 |
|------|:--:|:--:|:--:|
| 02-需求评审 | ❌ 跳过 | 可选 | 🔴 **应执行**（外部模型优先）｜`mandatory=false`，不阻塞推进 |
| 03-技术方案 | ❌ 跳过 | ✅ 强制 | ✅ 强制 |
| 04-开发实现 | ❌ 跳过 | ✅ 强制 | ✅ 强制 |

> 02 阶段 🔴 复杂级强制使用外部审查模型（优先调用 `review-gateway.py`，失败降级独立 Agent 审查）。🟡 中等级可选。🟢 简单级跳过。
> 03/04 涉及代码级审查（命名冲突/路径一致性/接口签名验证），必须强制互审兜底。

---

## 八、外部审查模型路由（v2.0）

> **设计目标**：审查使用第三方模型（如 SenseNova DeepSeek-V4），编码使用默认工具模型。第三方不可用时自动熔断降级，回退到默认 Agent 审查。

### 8.1 架构

```
主 Agent（默认模型，负责编排）
  │
  ├─ Step R1: 组装审查 prompt → 写入临时文件
  ├─ Step R2: Bash("python scripts/review-gateway.py --prompt-file <temp>")
  │            │
  │            ▼
  │       review-gateway.py（Python 脚本）
  │            │
  │            ├─ 读取 project/review-models.json（模型优先级列表）
  │            ├─ 读取 runtime/review-circuit.json（熔断状态）
  │            ├─ 循环: 按优先级试每个可用模型
  │            │   ├─ 熔断未过期 → 跳过
  │            │   ├─ 无 API Key → 跳过
  │            │   ├─ POST /v1/chat/completions
  │            │   │   ├─ 成功 → 清除熔断 → 返回 {"ok":true, "text":"..."}
  │            │   │   └─ 失败 → 打开熔断(TTL=5min) → 试下一个
  │            │   └─ 全部失败 → 返回 {"ok":false}
  │            └─ stdout: JSON 结果
  ├─ Step R3: 解析 stdout JSON
  │   ├─ ok==true  → 使用 text 作为审查结果，标注 "外部模型: {used}"
  │   └─ ok==false → ★ FALLBACK: 使用默认 Agent 自行审查，标注 "外部模型不可用，已降级"
  └─ Step R4: 合并审查结果 → 按 §五 合并规则处理
```

### 8.2 配置（project/review-models.json）

```json
{
  "version": 1,
  "default_ttl_minutes": 5,
  "models": [
    {
      "name": "sensenova-v4-flash",
      "base_url": "https://token.sensenova.cn/v1",
      "api_key_env": "SENSENOVA_API_KEY",
      "model": "deepseek-v4-flash",
      "priority": 1
    },
    {
      "name": "sensenova-6.7-flash-lite",
      "base_url": "https://token.sensenova.cn/v1",
      "api_key_env": "SENSENOVA_API_KEY",
      "model": "sensenova-6.7-flash-lite",
      "priority": 2
    }
  ]
}
```

- **priority**：数字越小越优先。熔断后自动跳到下一个。
- **api_key_env**：API Key 走环境变量（如 `SENSENOVA_API_KEY=sk-xxx`），不在此文件硬编码。
- **新增模型**：在 `models` 数组末尾追加对象即可，无需改脚本。

### 8.3 熔断机制（runtime/review-circuit.json）

```json
{
  "version": 1,
  "circuits": {
    "sensenova-v4-flash": {
      "open_since": "2026-07-18T14:30:00.123456+00:00",
      "fail_count": 3,
      "last_error": "HTTPError: 503 Service Unavailable",
      "ttl_minutes": 5
    }
  }
}
```

- 调用失败 → 自动写入熔断记录，TTL=5分钟
- TTL 未到期 → `review-gateway.py` 跳过该模型
- TTL 到期 + 下次调用成功 → 自动清除熔断
- 此文件**仅由 Python 脚本读写**，Agent 不应直接操作

### 8.4 Agent 执行步骤

当触发交叉审查（02：🔴 强制 / 🟡 可选；03/04 强制）时，主 Agent 自动检测外部模型配置并按本路由执行：

```
## 审查步骤（外部模型路由版）

Step 1: 检查 review-models.json 是否存在且 models 非空？
  → 不存在或为空 → 跳过，使用默认子 Agent 审查（零影响）
  → 存在且有模型 → 继续

Step 2: 组装审查 prompt（参考 §四 模板），写入临时文件
  → 写入路径: {skill_dir}/runtime/review-prompt-{stage}.txt

Step 3: 调用网关
  → 使用 Bash 工具执行:
     python scripts/review-gateway.py
       --prompt-file "{skill_dir}/runtime/review-prompt-{stage}.txt"
       --skill-dir "{skill_dir}"

Step 4: 解析 stdout JSON
  → 若 ok==true:
       - 将 text 作为审查报告
       - 输出标注: "📡 外部审查模型: {used} ({model})"
    若 ok==false:
       - ⚠️ FALLBACK: 启动 code-explorer 子 Agent 执行审查（原 §四 流程）
       - 输出标注: "⚠️ 外部模型全部不可用，已降级为默认 Agent 审查"

Step 5: 合并结果（按 §五 规则处理审查发现）
```

### 8.5 触发条件与降级

| 条件 | 行为 |
|------|------|
| 未配置 `project/review-models.json` 或 models 为空 | 跳过，使用默认子 Agent 审查 |
| 环境变量 `{api_key_env}` 未设置 | 跳过，使用默认子 Agent 审查 |
| Python 不可用 / 脚本不存在 | 跳过，使用默认子 Agent 审查 |
| review-gateway 返回 ok==true | ✅ 使用外部模型结果 |
| review-gateway 返回 ok==false | FALLBACK → 默认子 Agent 审查 |

> **渐进式采用**：不强制。仅当配置了 `review-models.json` 且 API Key 可用时自动启用。不配置 → 行为与现有完全一致（零影响）。

### 8.6 前置依赖

| 依赖 | 要求 | 检查方式 |
|------|------|---------|
| Python 3 | 本机可用 | `python --version` 返回 3.x |
| 环境变量 | `{api_key_env}` 已设置 | `os.environ.get(...)` |
| 网络 | 可连通 `{base_url}` | 脚本内 HTTP 调用失败即熔断 |

### 8.7 与独立 Agent 审查的并行关系

外部模型路由**替换** code-explorer 子代理审查（而非并行）。两种模式二选一：

| 模式 | 审查者 | 适用场景 |
|------|--------|---------|
| 默认模式（无外部模型） | code-explorer 独立 Agent（Claude） | 未配置 review-models.json |
| 外部模型模式 | review-gateway（第三方模型） | 已配置且有可用 API Key |

主 Agent 的自审步骤不受影响，始终执行。

---

## 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-12 | 1.0 | 初始版本，建立独立Agent交叉审查协议 |
| 2026-07-12 | 1.1 | 第二轮交叉审查修复：并行约定明确（步骤序号≠执行顺序）、合并规则精确化（不重新启动独立Agent）、边界场景覆盖（修复收敛条件/误报裁定权）、两套分级体系说明、L2/L3合并与主Agent一致、上下文描述修正 |
| 2026-07-12 | 1.2 | 第三/四轮全量一致性扫描修复：引导语对齐并行约定(F1)、文件命名对齐标准表(F2)、表格引用块截断修复(F3)、重复文字删除(F4)、版本号更新、步骤命名统一(F7) |
| 2026-07-18 | 2.1 | 🔴 02 阶段交叉审查升级为强制（外部模型优先）；复杂度门禁表同步更新 |
| 2026-09-20 | 2.2 | ★ 并行审查增强（决策 C）：新增 §二-B 分片与每路预算、§三-A 锚点包（禁止盲目全库探索）、§四 模板加锚点占位、§六 扩展为 N 路 + 汇总去重/冲突裁定规则 |
| 2026-09-20 | 2.2.1 | 独立审查（3 路分片）后订正：§二-A/§二-B 预算口径说明（合计 vs 每路，token=产出预算、调用次数=探索预算）、§二-B 明确"审查对象仍是全文，锚点约束的是探索范围"并声明**完整性/覆盖率维度不分片**、分片维度去掉已并入 L2 的 L3、§六 汇总表放宽"无行号即丢弃"为"行号或章节引用有其一即受理"、模板核验规则补"无 glob 的全局 search_content"并统一编号与表头 |
| 2026-09-20 | 2.2.2 | ★ **02 口径订正（P0 独立评估回流）**：§二 步骤 ID `step-1x-cross-review` → `step-1x-review`（与 SSOT/2026-07-18 v1.2 更名一致）；§二/§七/§4.3 的"🔴 强制"改为"**🔴 应执行**"并补注 `mandatory=false`（`mandatory` 权威源 = `complexity-scoring.md` §二，勿与"应执行"混为一维） |

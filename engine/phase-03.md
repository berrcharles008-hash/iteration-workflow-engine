# 阶段三：03-技术方案

**目标**：产出完整技术方案文档，覆盖前后端设计、API 协议、代码改动范围。

**执行方式**：主 Agent 分析代码库，结合需求产出方案。需要时派生 code-explorer 探索代码。

**★ 复杂度复评 `step-3-rescore`**（03 必经步骤）：探索完成后必须执行复评（用探索得到的**实际文件清单**重算复杂度），而不是"发现偏差才升级"。详见 §step-3-rescore。调整须用户确认后写入 `state.yaml` 的 `complexity` 与 **`complexity_adjustments`**（★ 字段名以 `state-protocol.md` schema 为准，**勿自造字段名**，P-048 同类）。

**🟢 简单需求极简模式**：产出独立 `03-技术方案.md`（极简 ~300字），聚焦改动范围表 + 接口签名，不含完整代码示例。内容模板见 `SKILL.md` 关键原则 #14。

### ★ 六步定位漏斗（step-1-explore 子步骤）

> 将原"全量 code-explorer 搜索"改为分层漏斗，逐级收敛 token 消耗。通过策略模式保证 L1/L2 不存在时自动回退，零改造代价。

**理论模型**：L1总览(~2K) → L2模块(~15K) → search_content(五维, 0) → read_file追踪(~10K) → read_file确认(~5K)，漏斗逐步收窄。

| 子步骤 | 功能 | 优先数据源 | 回退路径 | 预估Token |
|:------:|------|-----------|---------|:---:|
| step-1.1 | **意图消歧**：匹配需求关键词→候选模块 | `{{KB_DIR}}/L1-overview.md`（读取 config→kb_dir/l1_file） | specs 4文件 → list_dir | ~2K |
| step-1.2 | **模块定位**：锁定模块→精确文件清单（★多项目：遍历 `ALL_FRONTEND_ROOTS` + `ALL_BACKEND_ROOTS`） | `{{KB_DIR}}/L2-modules/{module}.md`（HTML注释解析 root_dirs；读取前 `--check` 校验新鲜度，过时先刷） | list_dir 扫描候选目录 | ~15K |
| step-1.3 | **关键词搜索**：五维展开搜索命中行 | `search_content`（类名/方法名/表名/路由/业务术语） | — | 0（Agent侧预过滤） |
| step-1.4 | **调用链追踪**：读取命中函数→展开调用链 | `read_file` 命中函数上下文 | — | ~10K |
| step-1.5 | **验证确认**：读取最终改动点→确认方案 | `read_file` 最终修改目标文件 | — | ~5K |
| step-1.6 | **知识库缓存**：L1 缺失/过时自动刷新（★多项目：遍历 `ALL_FRONTEND_ROOTS` + `ALL_BACKEND_ROOTS`，在L1模块表中标注归属项目） | 执行 `python scripts/gen-knowledge-base.py --level L1 --force` 刷新 `{{KB_DIR}}/L1-overview.md` | 跳过 | ~3K |

**策略模式回退规则**：
```
Step 1.1: {{KB_DIR}}/L1-overview.md 存在？→ 读 L1 : 读 specs 4 文件
           L1 存在但格式损坏？→ 输出警告 + 走回退路径
Step 1.2: ★ 读取前新鲜度校验（防御性兜底，覆盖同迭代内 04 改动场景）：
           python scripts/gen-knowledge-base.py --check --level L2
           若目标模块被标记"过时/缺失"→ 先执行 --force --module {module} 刷新，再读取
           {{KB_DIR}}/L2-modules/{module}.md 存在？→ 读 L2 : list_dir 扫描
           L2 存在但 root_dirs 缺失？→ 输出警告 + 走回退路径
Step 1.6: {{KB_DIR}}/L1-overview.md 不存在？→ 自动生成 : 跳过
           （自动生成逻辑等同 scripts/gen-knowledge-base.py --level L1）
```

**★ 多项目遍历逻辑**（`HAS_MULTI_PROJECTS = true` 时生效，单项目跳过本段）：

```
Step 1.2 模块定位回退路径（L2 不存在时）：
  对每个前端根目录（ALL_FRONTEND_ROOTS）：
    list_dir({root}/{frontend_layers.page.dir})  → 追加到候选
    list_dir({root}/{frontend_layers.bll.dir})   → 追加到候选
  对每个后端根目录（ALL_BACKEND_ROOTS）：
    list_dir({root}/{backend_layers.bll.dir})    → 追加到候选
    list_dir({root}/{backend_layers.entity.dir}) → 追加到候选

Step 1.6 L1 自动生成时：
  遍历 ALL_FRONTEND_ROOTS：
    扫描 {root}/{frontend_layers.page.dir}/*.vue
    → {{KB_DIR}}/L1-overview.md 前端模块表标注归属项目（路径前缀）
  遍历 ALL_BACKEND_ROOTS：
    扫描 {root}/{backend_layers.bll.dir}/*.cs
    → {{KB_DIR}}/L1-overview.md 后端模块表标注归属项目（路径前缀）
  
  若某项目的分层结构与主 frontend_layers/backend_layers 不一致：
    检查 manifest.paths.project_overrides[{项目目录名}]
    → 存在则使用覆盖值，不存在则使用主配置默认值
```

**🟢 简单迭代快速路径**：L1 命中唯一模块 + 无跨模块调用 → 跳过 step-1.3~1.4，仅保留 step-1.5 验证确认。

**产出**：`03-技术方案/{{DOC_03_TECHNICAL}}`（模板：`phase-03-技术方案.md`，优先级见启动协议 §模板解析优先级），必须包含：
- 现状与痛点
- 改造方案（DDL、数据流、页面设计）
- 代码改动范围（Delta 标记体系，见 `engine/delta-marking.md`）— 含 ADDED/MODIFIED 标注
- API 接口协议（路由、参数、返回值、接口签名）— **必须**
- 开发阶段与任务拆解

**🔴 复杂方案的代码示例策略**（与项1 关键符号验证协同，降 token / 提可靠）：
- 方案聚焦「**改动范围表 + 接口签名**」；完整前后端代码示例改为**可选/按需**，仅在关键算法或新模式时附最小可运行片段，避免与实际编码偏离及 token 浪费
- 🟢/🟡：聚焦改动范围表 + 接口签名即可，代码示例可选
- **04 Team Agent 派发硬规则**：使用 Team Agent 派发时，task prompt **必须**含「改动范围表 + 接口签名 +（关键算法/新模式的最小可运行片段）」；普通 CRUD 仅需接口签名

**关键原则**：
- API 接口协议必须在方案中明确定义，前后端对齐
- 标注每个文件是"新增"还是"修改"以及改动类型（ADDED/MODIFIED-追加/修改/替换）
- **★ 命名冲突预检**（方案输出前强制）：详见 `engine/naming-conflict-check.md`

### ★ 自主审查（强制，输出方案后、交付前执行）

> 方案输出完成后，Agent **必须自行执行以下两层审查**，全部通过后才能向用户提交确认。
> 审查必须读取实际代码文件进行交叉比对，不得仅凭记忆判断。
> 审查必须输出明确的通过/发现问题的结论。

| 层级 | 检查项 | 执行方式 |
|:--:|------|------|
| L1 | **命名冲突**：新增的方法/类型/变量名是否与目标文件中已存在的符号重复 | 搜索目标文件中的同名符号 |
| L2 | **关键符号验证**：方案中引用的关键符号是否在实际代码库中存在 | 从改动范围表提取符号，最多验证 5 项（优先级排序见下） |

**L2 关键符号验证规则**（★ 合并原 L2 内部自洽 + L3 代码库交叉验证）：

1. **从方案文档的「代码改动范围」表提取所有新增/修改的符号**（类名、方法名、异常类、路由名等）
2. **按优先级排序，最多取 5 项**：
   - 优先级 1（必验）：异常类（`XxxException`）、接口签名（`IMgr` 方法）
   - 优先级 2（必验）：继承基类（`: {{backend_layers.bll.base_class}}` / `: {{frontend_layers.net.base_class}}`）、路由名（`[WebApi("...")]`）
   - 优先级 3（按需验）：前端类名大小写、参数绑定方式、权限前提
3. **每项用 `search_content` 在代码库中搜索一次**，确认实际存在
4. **超过 5 项时**：只验前 5 项，其余标注"⚠️ 建议后续验证"

> **★ 禁止行为**：Agent 完成方案输出后，**不得直接问用户"是否确认通过"**。必须先完成 L1→L2 两层审查，输出审查结论后，再邀请用户确认。
>
> 事故案例：2026-06-30-007 迭代中，Agent 仅做 L1 审查就邀请用户确认，遗漏 L2（DeleteResult 未列入）和 L3（BusinessException 不存在、前端类名大小写错误、DBMS_LOCK 权限未标注），导致方案存在编译级错误。

> **为什么合并 L2+L3**：原 L3 有 6 项验证清单，每项都要 `search_content`，token 消耗巨大且 Agent 容易疲劳跳过。合并后用"优先级 + 最多 5 项"控制验证深度，确保最重要的符号一定被验到。

### ★ 独立 Agent 交叉审查（强制，🟡🔴）

> 主 Agent 先启动独立 Agent（code-explorer 子代理），再立即执行自身自审（两者同时启动，互不阻塞）。
> 独立 Agent 不受作者盲区影响，能发现主 Agent 自审遗漏的问题。

**审查内容**：L1 命名冲突 + L2 关键符号验证（与自审相同维度，独立执行，最多5项）。

**执行流程**：
1. 主 Agent 启动独立 Agent，传入审查指令模板（详见 `engine/cross-review-protocol.md` §4.1）
   - ★ **派发前先组装锚点包**（待验符号 → 绝对路径 → 行号，来源 = 方案「代码改动范围」表），🔴 复杂级按模块/维度分 2~4 路、每路 ≤40 次工具调用；禁止子 Agent 全库 `rglob`。详见协议 §二-B / §三-A / §六
   - 运行时将 `{{方案文档路径}}` 替换为 `{ITERATIONS_DIR}/{ID}/03-技术方案/{{DOC_03_TECHNICAL}}` 的绝对路径
   - `{{PROJECT_ROOT}}` 替换为项目根目录绝对路径
2. 独立 Agent 读取方案文档 + 搜索代码库 → 输出分级审查报告（🔴致命/🟡严重/🔵轻微/💡建议）
3. 主 Agent 按合并规则处理（🔴🟡→必须修复，🔵→可选，💡→记录）

**并行约定**：独立 Agent 与主 Agent 自审**同时启动**（主 Agent 先 `task` 派发独立 Agent，再立即执行自身自审），互不阻塞。两份结果返回后统一合并。⚠️ 步骤清单中的序号不代表执行顺序。

**适用复杂度**：🟡🔴（🟢 跳过，改动量极小的交叉审查 token 成本 > 收益）。

> 为什么自审 + 互审双保险？自审覆盖业务理解和设计意图（独立Agent做不到），
> 互审覆盖盲区和遗漏（自审做不到）。两者互补，不是替代。

### ★ 异步时序分析（强制，涉及跨组件/跨生命周期数据流时执行）

> 解决问题：Agent 在 03 方案阶段只做"表面代码逻辑走读"（看通路、看分支），
> 没有深入到框架机制层分析异步时序，导致真正的根因延迟到 04 开发阶段才被发现。
> 具体事故案例和当前项目框架的 API 映射见项目层时序指南：`project/{{frontend_framework}}-async-timing-patterns.md`

**触发条件**：方案涉及以下任一场景时，必须执行异步时序分析：
1. 父子组件/模块通过框架引用机制交互（子组件引用、跨组件数据操作）
2. 跨生命周期钩子的数据依赖（挂载/创建/副作用钩子/侦听器）
3. 子组件/模块初始化时有异步 API 请求或数据加载
4. 父组件在自身初始化阶段操作子组件的数据或方法

**分析步骤**：
1. **画异步时序图**（Mermaid sequenceDiagram）：标注每个 `await` 断点、数据传播点、跨组件调用点
2. **标注数据前提条件**：每个跨组件调用点需要哪些数据已就绪
3. **验证前提条件**：对照时序图，逐一检验数据就绪时间 vs 调用时间
4. **未满足的前提 → 增加等待步骤**（如等待子组件异步初始化完成、等待下一渲染周期）

**检查清单**（自主审查时逐项确认；具体 API 映射见项目层时序指南）：
- [ ] 子组件是否有异步初始化（框架挂载/创建钩子中的 await 调用）？→ 父组件不能假设其已完成
- [ ] 父组件修改数据后是否立即操作子组件？→ 需等待下一渲染周期
- [ ] 子组件是否有异步初始化方法？→ 父组件必须先等待它完成
- [ ] 模拟延迟下时序是否仍然正确？→ 关键路径建议用延迟验证

> 当前项目（{{frontend_framework}}）的具体 API 映射和事故案例见：`project/{{frontend_framework}}-async-timing-patterns.md`

### ★ 评审边界与通过标准（强制，自主审查完成后执行）

> 解决问题：Agent 在 03 方案阶段反复评审，每次都发现新问题（编译错误、UX、权限、配置...），
> 把本该属于"代码审查"的问题提前到"方案评审"，导致评审无限循环、效率低下。
> 核心机制：明确方案评审的检查范围和通过条件，超出范围的问题不在方案阶段处理。

#### 评审检查项（仅以下4项，不扩展）

| # | 检查项 | 不通过的标准（致命问题） |
|---|--------|----------------------|
| 1 | 架构合理性 | 数据流断裂、分层错误、依赖方向错误、缺失关键依赖 |
| 2 | 设计决策正确性 | 策略选择有明显更优方案且未说明理由、关键决策依据错误 |
| 3 | 安全漏洞 | SQL注入、权限绕过、数据泄露、删除未做安全防护 |
| 4 | 遗漏关键场景 | 未处理异常路径、未考虑并发/超时/部分失败 |

**不在方案评审范围内**（留待04阶段编码/编译/代码审查处理）：
- 变量命名、类型签名、import 语句缺失
- 编译错误（如同名成员、类型不匹配）
- 注释措辞、文档一致性
- UX 细节（loading 状态、空值显示）
- 配置确认（NLog 目标、权限框架）
- 性能微调

#### 通过条件

```
通过条件：4 项检查全部无致命问题
不通过条件：任一检查发现致命问题
```

非致命问题归类为"📝 实现注意"，记录在方案末尾但不阻塞进入04阶段。

#### 问题分级（简化为2级，去掉模糊的"建议修复"）

| 级别 | 含义 | 处理方式 |
|------|------|---------|
| 🔴 阻塞 | 架构缺陷/安全漏洞/设计错误/遗漏关键场景 | 必须修复后才能进入04 |
| 📝 注意 | 实现细节/UX优化/配置确认/性能微调 | 记录，04编码时处理，不阻塞 |

#### 评审轮次限制

| 阶段 | 评审类型 | 最大轮次 |
|------|---------|:--:|
| 03 方案 | 架构评审 | 1轮（用户可手动追加1轮） |
| 04 编码后 | 代码审查 | 1轮（编译器+人工） |
| 05 测试前 | 集成审查 | 1轮 |

**方案阶段默认只做1轮评审。** 如用户未明确要求再次评审，不主动发起。

#### 评审输出格式

```
## 03 方案评审结论

| 检查项 | 结果 |
|--------|------|
| 1. 架构合理性 | ✅/❌ {说明} |
| 2. 设计决策正确性 | ✅/❌ {说明} |
| 3. 安全漏洞 | ✅/❌ {说明} |
| 4. 遗漏关键场景 | ✅/❌ {说明} |

📝 实现注意（04阶段处理）：
- {非致命问题1}
- {非致命问题2}

评审结论：通过 / 不通过（{原因}）
```

### ★ 修复验证（Fix Verification，强制，每次修复后执行）

> 解决问题：Agent 修复评审问题后直接输出"全部完成"，未验证修改是否完整、是否引入新问题、
> 相关文档是否同步，导致修复本身引入新缺陷（如改名后同名冲突、描述与代码不一致）。
> 核心机制：每次修复（无论03方案还是04代码）后，必须执行3项验证，全部通过才算完成。

#### 适用范围

**所有阶段的修复操作**，包括但不限于：
- 03 方案评审问题修复后
- 04 编译错误修复后
- 04 代码审查问题修复后
- 05 测试失败修复后

#### 验证清单（3项，缺一不可）

| # | 验证项 | 执行方式 | 不通过的处理 |
|---|--------|---------|------------|
| 1 | **修改完整性** | 搜索所有引用点，确认都同步修改（如方法改名→搜索所有调用点） | 补全遗漏的引用点 |
| 2 | **未引入新问题** | 检查改动点是否与现有代码/方案冲突（同名成员、类型不匹配、逻辑矛盾） | 修复新引入的问题 |
| 3 | **文档同步** | 相关描述/注释/表格是否与改动后的代码一致 | 同步更新文档 |

#### 执行流程

```
发现问题 → 修复 → 修复验证（3项）
              ↓
              ① 修改完整性：搜索所有引用点 → 全部同步？
                 ├── 否 → 补全 → 重新验证①
                 └── 是 → ②
              ② 未引入新问题：检查同名/类型/逻辑冲突？
                 ├── 有 → 修复 → 重新验证②
                 └── 无 → ③
              ③ 文档同步：描述/注释/表格一致？
                 ├── 否 → 同步更新 → 重新验证③
                 └── 是 → ✅ 验证通过
              ↓
          输出验证结论 → 继续
```

#### 输出格式

```
## 修复验证（Fix Verification）

| # | 验证项 | 结果 | 说明 |
|---|--------|:--:|------|
| 1 | 修改完整性 | ✅ | 搜索 N 处引用，全部同步 |
| 2 | 未引入新问题 | ✅ | 无同名/类型/逻辑冲突 |
| 3 | 文档同步 | ✅ | 相关描述已同步更新 |

验证结论：通过
```

**禁止行为**：
- ❌ 修复后直接输出"全部完成"而不执行验证
- ❌ 只验证修改点本身，不检查引用点和文档
- ❌ 验证发现新问题后不修复就继续

> 事故案例：2026-06-30-007 迭代03阶段，Agent 修复 P2（统一命名 EstimateCount）后未验证引用点，
> 导致数据属性 `estimateCount` 与方法 `estimateCount()` 同名（P9），直到下一轮评审才被发现。

> **版式纪律**：产出文档须遵循 `engine/doc-style-guide.md`（代码骨架/SQL/审查处置记录进附录，正文只留签名与结论）；生成后自检 `python scripts/doc_lint.py <文件>`（ERROR 必修）。

**交付标准**：三层自主审查通过 + 4项评审检查通过 + 修复验证通过 + 方案覆盖所有改动点 + 前后端对齐 + 用户确认"方案通过"；**文档版式自检 ERROR 0**（`python scripts/doc_lint.py <文档>`，含 `## 目录` / 相对链接有效性 / Mermaid）

### ★ step-3-rescore：复杂度复评（03 必经步骤）

> **背景**：Step C 初评发生在 01 阶段（无代码探索信息）⇒ **初评本就是草评**。03 探索完成后信息最全，是最有效的校准点。
> 本步骤将原来的"发现偏差才升级"改为**必经复评**，避免 Agent 静默硬撑。

**执行时机**：`step-1-explore`（含 1.1~1.5）完成后、`step-2-output` 产出方案之前。

**输入**：探索得到的实际改动文件清单 + 是否触及 DB / 新接口 / 跨模块。

**固定输出表（必须原样输出，不得省略）**：

| 项 | 值 |
|---|---|
| 初评等级 | {🟢 / 🟡 / 🔴} |
| 实际因素命中 | {因素 id + 各自得分} |
| 加权总分 | {N} |
| 硬下限是否触发 | {是 / 否 —— 命中 §1.2.1 五因素之一即为"是"} |
| 复评等级 | {🟢 / 🟡 / 🔴} |
| 与初评差异 | {一致 / 升级 X→Y / 降级 X→Y} |

**分支处理**：

| 差异 | 处理 |
|------|------|
| 一致 | 记录"一致"后继续 `step-2-output`，**无需用户确认** |
| **升级** | 提示用户 + 说明新增的强制项（如 `step-3x-cross-review`、`step-1-5x-cross-review`）；用户确认后写入 `complexity` + `complexity_adjustments`，按新等级执行 |
| **降级** | **必须显式提示"降级会削弱审查强度"**（如取消交叉审查），且 **不得跨两级**（🔴→🟢 禁止）；用户确认后同上写入 |

**覆盖硬下限**：用户坚持维持低于硬下限的等级时，Agent 须告知后果，并在 `complexity_adjustments.reason` 记录 `"用户覆盖硬下限：{原因}"`。

### 步骤清单（进入阶段时写入 phase_steps）

> 完整步骤清单及 gate 写入要求详见 `phase-steps.md` §阶段三。

| 步骤ID | 步骤名称 | 强制 | 触发条件 |
|--------|---------|:--:|---------|
| step-1-explore | 代码探索（容器步骤，实际执行由子步骤条件决定） | — | 始终 |
| step-1.1 | 意图消歧（L1匹配→候选模块） | — | 始终 |
| step-1.2 | 模块定位（L2读取→精确文件清单） | — | 始终 |
| step-1.3 | 关键词搜索（五维 search_content） | — | 始终（🟢简单快速路径可跳过） |
| step-1.4 | 调用链追踪（read_file 命中函数） | — | 始终（🟢简单快速路径可跳过） |
| step-1.5 | 验证确认（read_file 最终改动点） | ✅ | 始终 |
| step-1.6 | 知识库缓存（L1 不存在时自动生成） | — | L1 不存在时 |
| step-3-rescore | 复杂度复评（实际文件清单重算，见 §step-3-rescore） | ✅ | 始终（🟢 可简评，输出表不得省略） |
| step-2-output | 产出技术方案文档 | ✅ | 始终 |
| step-3-self-review | 自主审查L1/L2（关键符号验证） | ✅ | 始终 |
| step-3x-cross-review | 独立Agent交叉审查L1/L2 | ✅ | 🟡🔴 |
| step-4-review-gate | 评审边界与通过标准检查（4项） | ✅ | 始终 |
| step-5-fix-verify | 修复验证（3项） | ✅ | 审查发现问题后 |
| step-5-review-gate-result | 写入评审门禁结果到 state.yaml | ✅ | 始终 |
| step-6-user-confirm | 用户确认方案通过 | ✅ | 始终 |

> ⚠️ **步骤序号 ≠ 执行顺序**：step-3x 排在 step-3 之后仅为编号有序。实际执行时互审与自审**同时启动**（见 §独立Agent交叉审查 并行约定）。

---
name: iteration-workflow
description: |
  迭代开发工作流 Skill。覆盖7个标准阶段：01-需求分析与设计、
  02-需求评审、03-技术方案、04-开发实现（Team Agent模式）、05-测试验证、06-发布上线、07-迭代回顾。
  典型触发词：开始迭代、进入XX阶段、执行Sprint、按迭代流程、修改代码、改代码、
  帮我修改、改一下、开始实施、开始开发、开始写代码、发布到测试服务器、部署到测试服务器。
  ★ 兜底：任何涉及代码/文件修改、迭代管理、需求分析的意图（即使未命中以上关键词）
  ★ 状态查询类（查进度/查阶段/查迭代状态）
  ★ 多 Story 并行模式触发词
version: "2.2.1"
config:
  specs_dir: references/specs/
  specs_fallback_dirs:
    - .codebuddy/specs/
  # 活文档知识库根目录（L1/L2/L3 自动生成）
  # 读取路径: {kb_dir}/{l1_file}, {kb_dir}/{l2_dir}/, {kb_dir}/{l3_file}
  kb_dir: docs/knowledge-base/
  l1_file: L1-overview.md
  l2_dir: L2-modules/
  l3_file: L3-glossary.md
---

## 文件加载优先级

> 非同名追加文件由本 SKILL.md 的读取顺序引导加载。

| 优先级 | 目录 | 内容 |
|:----:|------|------|
| 1 | `engine/`（核心协议文件） | 核心引擎协议文件 |
| 2 | `project/` | 项目配置 |
| 3 | `runtime/` | 运行时状态 |

### 启动读取顺序

```
1. 读 engine/startup-protocol.md Step A-D（通用启动流程）
2. 读 engine/startup-protocol-step-e.md Step E（每日工作日志）
3. 模板路径：按启动协议 §模板解析优先级 解析（先 project/templates/ 后 engine/templates/）
4. 文档版式：engine/doc-style-guide.md（01~07 阶段产出统一遵循；生成后跑 scripts/doc_lint.py 自检）
```

## ★ 上下文纪律（定点读 · 反全文入上下文）

> **"读什么"由启动协议规定；"读多少"由本条约束。** 读入内容会**逐轮重放**，故大文件一律定点读。
> 完整细则与各阶段应用要点见 `engine/context-discipline.md`。

| # | 对象 | 纪律 |
|:--:|------|------|
| 1 | 规格/计划类（`requirements/`、`03-技术方案.md`、任务清单） | 先 `search_content` 定位 → 再 `read_file offset/limit` 只读命中区间 |
| 2 | 状态文件（`state.yaml`） | 只读 `current_phase` / `phase_steps` / `tasks_*` 区间（~50 行），禁全文回读 |
| 3 | 源码 | 仅在"即将编辑 / 落盘前核对"时读，只读待替换片段，禁预读整个类文件 |
| 4 | 引擎文件 | 只读**当前步骤**涉及的文件，禁"预读全套" |
| 5 | 大产物（>300 行） | 要求产出方给"摘要 + diff 片段" |

**自检指标**：单轮 `read_file` 合计 **≤ 500 行**（跨阶段整段重读等例外须注明理由）。
**兜底**：`read_file` 报 "File too large" ⇒ 直接转 `search_content` + `offset/limit`，**禁**换工具硬读全文。

## ★ 第一优先级：轻量查询快速退出

> **本 skill 被加载后，立即执行此判断——在读取任何其他文件之前。**

| 用户意图 | 匹配关键词 | 执行路径 |
|---------|-----------|---------|
| **状态查询** | 当前进度、现在什么阶段、当前处在哪个迭代、查看迭代状态、查进度、查阶段 | **→ 快速退出**：仅读取 `runtime/ACTIVE`（2行），解析 Line 2 的 `STATUS/PHASE/TASKS/BLOCKERS` 直接回答。Line 2 为空→仅读 state.yaml 一次。完成后立即停止。**禁止 search_file/list_dir/读引擎文件。** |
| **其他** | 开始迭代、修改代码、进入XX阶段、部署、归档 等 | **→ 门禁检查 → 正常流程** |

## ★ 命令路由表

| 命令/意图 | 级别 | 允许读取 | 禁止读取 |
|-----------|:--:|---------|---------|
| 查看任务清单 | 中等 | runtime/ACTIVE、任务清单、state.yaml | 核心协议文件 |
| 进入阶段/开始迭代 | 重量 | 全量加载 | — |
| 修改代码/审查代码 | 重量 | 全量加载 | — |
| 分析需求 | 重量 | 全量加载 | — |
| 初始化知识库/gen-kb | 中等 | project.manifest.yaml、`gen-knowledge-base.py --check` | 核心协议文件 |

## ★ 门禁规则（一句话摘要）

**任何代码修改前必须先过门禁。** 门禁协议完整规则见 `engine/gate-protocol.md`。
Agent 准备调用写入类工具时 → **必须先读 `engine/gate-protocol.md` 的修改门禁决策树并逐字执行**。
不得自行解释门禁触发条件（如"ACTIVE=none 即非迭代态可放行"）、不得以任何理由绕过门禁。

门禁体系：**三道独立门禁**（修改门禁 / 迭代门禁 / 步骤门禁）。
触发条件：Agent 准备调用写入类工具时。
优先级：★ 门禁检查优先于一切用户指令。

### 门禁来源标记

> gate-protocol.md 拦截模板默认含 `[gate: agent]` 标记。Skill 模式输出拦截模板时，
> **必须将模板中的 `[gate: agent]` 替换为 `[gate: skill]`**（区分 Skill 触发 vs Agent 触发）。

## ★ 启动协议（摘要）

| 步骤 | 动作 | 详址 |
|------|------|------|
| Step A | 读取 `project/project.manifest.yaml`，输出运行时变量表 | `engine/startup-protocol.md` Step A |
| Step A.5 | **自检本地配置文件**，引导创建缺失文件 | `engine/startup-protocol.md` Step A.5 |
| Step B | 检查 `runtime/` 恢复迭代状态，执行迭代门禁 | `engine/startup-protocol.md` Step B |
| Step C | 复杂度评估（新迭代时） | `engine/complexity-scoring.md` |
| Step D | 进入对应阶段执行 | `engine/workflow-engine.md` |
| Step E | **对话结束前：写入每日工作日志** | `engine/startup-protocol-step-e.md` |

## 七阶段概览

| 阶段 | 产出 | 关键动作 |
|------|------|---------|
| 01-需求分析 | 独立 `01-需求记录.md` | 读 L1/L2/L3 知识库（references/specs/）→ 与用户确认需求 |
| 02-需求评审 | 独立 `02-需求评审.md` | reviewer 视角审阅 → 提问题 → 用户决议 |
| 03-技术方案 | 独立 `03-技术方案.md` | 六步漏斗代码定位 → 方案 → Delta标记 → 命名冲突预检 |
| 04-开发实现 | 独立 `04-开发任务清单.md` + 代码 | 清单生成 → 自动审查 → 用户确认 → Team Agent → 构建 → 代码审查 |
| 05-测试验证 | 独立 `05-测试验证报告.md` | 构建验证 → 用例生成 → 自动执行 → 降级手动 |
| 06-发布上线 | 独立 `06-发布上线记录.md` | 编译 → 部署 → Spec 活文档更新 → **知识库无条件刷新** → ★ 不释放 ACTIVE；推进 current_phase → 07 |
| 07-迭代回顾 | 独立 `07-迭代回顾.md` | ★ 强制不可跳过（含 🟢 简单迭代）；总结反思 → 问题清单 → 模式沉淀 → 归档释放 |

## 关键原则

0. **★ 修改门禁强制**：任何代码修改前，必须先通过门禁检查。未通过门禁不得修改业务代码。
1. **阶段不可跳过**：必须按 01→07 顺序执行。🟢 简单需求下 02/03 采用**极简模式**（产出独立文件，内容极简化），但阶段核心逻辑不得完全跳过：02 极简逻辑为需求范围确认 + 4 问判断（是否需 03 方案）；03 极简逻辑为六步漏斗定位、关键符号验证、命名冲突预检（产出独立 `03-技术方案.md`，极简 ~300字）。
2. **文档驱动**：每个阶段的产出文档是下一阶段的输入。
3. **04 阶段必须用 Team Agent**（🟢简单需求除外）。
4. **任务清单是唯一真相源**：进度完全由任务清单的勾选状态决定。
5. **任务清单强制审查**：每次任务清单生成/更新后，必须与 03-技术方案 逐项对照。
6. **编译单元注册+构建验证强制**：涉及源文件新增/删除时，必须更新编译单元注册 + 执行构建验证。
7. **构建验证强制**：涉及代码变更的迭代，05 阶段必须先执行构建命令确保 0 编译错误。
8. **逐阶段确认**：每个阶段完成后，必须等待用户明确确认。
9. **能跑则跑，不能则降级**：05 阶段自动生成用例 → 探测环境 → 能自动的执行 → 不能的标记为手动。
10. **命名冲突预检强制**：涉及在已有代码中新增符号时，03 和 04 阶段各执行一次命名冲突扫描。
11. **★ 图优先原则**：涉及架构图、时序图等可视化内容时，优先使用 Mermaid 图表嵌入文档。
12. **★ 独立Agent交叉审查（自审+互审双保险）**：🔴 复杂级 02/03/04 阶段启动独立 Agent 交叉审查；🟡 中等级 03/04 强制、02 可选。主 Agent 在需要时先启动独立 Agent，再立即执行自审。详见 `engine/cross-review-protocol.md`。
★ **"应执行" ≠ "mandatory"（两个维度）**：03/04 的交叉审查 = `mandatory=true`（门禁强制、不做即拦）；**02 = `mandatory=false`**（🔴 应执行但**不阻塞推进**）。`mandatory` 的**唯一权威源 = `complexity-scoring.md` §二**。
★ 派发前须组装**锚点包**（符号→绝对路径→行号）并按模块/维度**分片**（🟡2 路 / 🔴2~4 路，每路限工具调用次数），禁止子 Agent 无目标全库探索（协议 §二-B / §三-A）。
13. **★ 外部模型审查路由（v2.0.0）**：🔴 复杂级的 02 阶段强制使用，03/04 阶段优先使用。详见 `engine/cross-review-protocol.md` §八。
14. **★ 每个阶段必须有独立目录产出**：复杂度只决定内容详细程度，不决定文件位置。🟢 简单：独立文件，内容极简（02 ~200字 / 03 ~300字 / 07 精简三段式）；🟡🔴：独立文件，标准/完整内容。
15. **★ 知识库自动兜底**：`{{KB_DIR}}` 下 L1/L2/L3 由 `scripts/gen-knowledge-base.py` 维护（`--check` 检测缺失/过时 + **文件指纹**（mtime_ns+size，检出既有文件被改动），`--force` 刷新）。01 前置步骤与 step-1.6-dir-diff 发现缺失/过时时**自动刷新**（读前兜底，不等待用户确认）；03 step-1.2 读取 L2 前做新鲜度校验；**04 不刷新**（代码未验证、无消费者）；**06 `step-2-5-kb-refresh` 无条件刷新 = 最终态**（归档前）。人工编辑文件（`auto-generated: false`）永不被覆盖。
16. **★ 上下文纪律（定点读）**：大文件禁全文入上下文 —— 先定位后定点读、状态文件只读关键区间、源码只读待改片段、单轮 `read_file` ≤ 500 行。详见 `engine/context-discipline.md`。
17. **★★ 引擎自身修改 = 「引擎演进安全协议」**（用户 2026-09-20 立法，最高优先级）：**任何**对本 skill 的修改（`engine/**`、`scripts/**`、`hooks/**`、`SKILL.md`、模板等）**禁止"先改后报"** —— 四件套前置审批、核心文件快照、收口验证（3 脚本 + 回流水位）**全部**见 `engine/evolution-safety.md`，此处**不再复述**（★ 单点权威，防两份口径漂移）。

> 完整流程定义见 `engine/workflow-engine.md`，门禁规则见 `engine/gate-protocol.md`。
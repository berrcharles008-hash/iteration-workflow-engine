# 5 分钟快速入门

> 这不是完整的 Skill 文档，而是认知地图——告诉你去哪找什么。

## 一、一句话定位 [10s]

`iteration-workflow` 是一个 AI 辅助的迭代开发流程引擎。
你告诉 Agent"我要开始一个新需求"，它按 7 个阶段引导你——从需求分析到上线回顾。

---

## 二、流程概览 [2min]

### 七阶段环

```
01-需求分析 → 02-需求评审 → 03-技术方案 → 04-开发实现
                                                 ↓
              07-迭代回顾 ← 06-发布上线 ← 05-测试验证
```

每个阶段产出对应的文档（`需求文档.md` / `技术方案.md` / `测试报告.md` 等），
前一阶段的产出是下一阶段的输入。

### ACTIVE 指针

`runtime/ACTIVE` 文件记录"当前活跃的迭代 ID"。会话启动时 Agent 读取它，
自动恢复到上次中断的位置。**同一时间只有一个活跃迭代**。

### 任务清单

开发阶段（04）生成 `开发任务清单.md`，里面是按模块拆分的待办任务。
**进度完全由任务清单的勾选状态决定**——不打勾就是没做完。

---

## 三、关键规则 [1min]

### ★ 门禁铁律

**任何代码修改前，必须先过门禁检查。** 这是硬约束，三层防线：

| 门禁层 | 触发时机 | 作用 |
|--------|---------|------|
| 修改门禁 | Agent 准备调用写入工具时 | 确认存在活跃迭代、在 04 阶段 |
| 迭代门禁 | 用户请求"开始新迭代"时 | 检查无冲突的进行中迭代 |
| 步骤门禁 | 阶段推进时 | 确认上一步骤的 mandatory 项已完成 |

> **教训**：2026-07-15，"开始实施"不在触发词列表导致门禁被绕过。
> 如果你收到门禁阻止提示，说明保护机制正常工作——不是故障。

---

## 四、第一次使用 [1min]

### ⚠️ 前置步骤：注入冷启动门禁微核（CodeBuddy IDE 用户）

如果你使用 **CodeBuddy IDE**（非 Claude Code CLI），首次安装后需执行一次：

```bash
cd .codebuddy/skills/iteration-workflow
python scripts/setup-gate.py
```

> Claude Code CLI 用户可跳过——Hook 层已提供硬拦截保护。
> 也可在首次对话中由 Step A.5 自动检测并提示注入。

### 新项目初始化向导

在新项目中接入时，运行交互式向导自动完成全套配置：

```powershell
cd .codebuddy/skills/iteration-workflow
pwsh scripts/init-checklist.ps1 -ProjectPath "你的项目路径"
```

向导会引导你完成：`project.manifest.yaml` 项目配置、编码规范、审查规则、门禁微核注入等。
也可手动编辑 `project/` 下的配置文件（见下节文件地图）。

### 典型对话

```
你：开始一个新迭代
Agent：→ 创建迭代 → 进入 01 需求分析 → 问你需求细节
你：（描述需求）
Agent：→ 生成需求文档 → 进入 02 需求评审 → 评审结果
你：通过
Agent：→ 进入 03 技术方案 → 探索代码 → 生成方案
你：确认方案
Agent：→ 进入 04 开发实现 → 生成任务清单 → 逐一实现
你：确认任务完成
Agent：→ 进入 05 测试验证 → 生成用例 → 自动执行
...
```

### ⚠️ 你需要参与确认的阶段

| 阶段 | 你的介入点 |
|:---:|------|
| 02 需求评审 | 审阅 Agent 的评审结果，决定通过/修正 |
| 03 技术方案 | 确认方案方向是否可行 |
| 05 测试验证 | 手动测试 Agent 无法自动执行的用例 |
| 06 发布上线 | 确认部署完成 |

**Agent 不会全自动跑完全程**——关键决策点必须等你确认后才进入下一阶段。

---

## 五、常用意图速查 [30s]

| 你想做的事 | 对 Agent 说 | 会发生什么 |
|-----------|-----------|-----------|
| 看当前进度 | "当前什么阶段？" | 只读 `ACTIVE` 文件，瞬间返回 |
| 看任务清单 | "查看任务清单" | 读 `ACTIVE` + 任务清单 + `state.yaml` |
| 开始新需求 | "开始一个新迭代" | 全量加载，创建迭代，进入 01 阶段 |
| 修改代码 | "帮我改一下 XX" | 先过门禁 → 确认在正确的阶段 → 执行修改 |
| 继续上次 | "继续迭代" | 恢复上次中断位置的上下文 |
| 发布部署 | "部署到测试服务器" | 进入 06 阶段流程 |
| 归档 | "归档迭代" | 06 阶段归档 → 进入 07 回顾 |

---

## 六、文件地图（核心 6 个） [30s]

| 文件 | 干什么的 | 什么时候看 |
|------|---------|-----------|
| `SKILL.md` | Skill 入口，路由表 + 原则摘要 | 首次了解 |
| `engine/gate-protocol.md` | 三道门禁完整规则 | 想知道"为什么改不了代码" |
| `engine/workflow-engine.md` | 七阶段详细定义 | 想深入理解某个阶段 |
| `engine/state-protocol.md` | 状态文件格式与读写规则 | 排查状态文件问题 |
| `engine/phase-steps.md` | 各阶段步骤清单（唯一真源） | 确认某阶段的步骤是否完整 |
| `project/project.manifest.yaml` | 项目配置（路径/技术栈/编译单元） | 新项目接入 |

> 完整文件清单 48 个，日常使用只需这 6 个。

---

## ⚠️ 常见误解 [30s]

| 误解 | 实际情况 |
|------|---------|
| "Agent 会全自动完成所有阶段" | 不会。02/03/05/06 阶段你必须参与确认和决策。 |
| "门禁阻断 = 出 bug 了" | 门禁阻断是**保护机制正常工作**。没活跃迭代就改代码可能造成数据混乱。 |
| "任何说法都能触发 Skill" | 虽然已大幅拓宽触发词，但极端边缘的表述仍可能漏掉。如果你说了改代码但 Agent 没走门禁，说"先过门禁"即可纠正。 |
| "暂停的迭代会自动恢复" | 暂停后你需要明确说"恢复迭代"才会继续，不会自动悄悄继续。 |
